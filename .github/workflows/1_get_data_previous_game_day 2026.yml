name: "🏀 1. Collect and Prepare NBA Game Data (Previous Game Day)"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 20 * * *"   # example: run daily 20:00 UTC, keep whatever you already use

jobs:
  collect_data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (branch 2026)
        uses: actions/checkout@v4
        with:
          ref: 2026

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Google Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Scrape and prepare PREVIOUS game day data
        run: |
          echo "=== Starting scrape for PREVIOUS game day ==="
          python 2026/src/1_get_data_previous_game_day_2026.py
          echo "=== Script finished ==="

      - name: Upload artifact (previous game day data)
        uses: actions/upload-artifact@v4
        with:
          name: previous_game_day_data
          path: |
            2026/output/Gathering_Data/**
            !2026/output/Gathering_Data/Next_Game/**

      - name: Commit and push scraped data back to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add 2026/output/Gathering_Data

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            commit_msg="auto-update scraped game data ($(date -u +%Y-%m-%dT%H:%MZ))"
            git commit -m "$commit_msg"
            git push origin 2026
          fi
